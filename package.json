{
  "name": "mcp",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server:build": "tsc",
    "server:build:watch": "tsc --watch",
    "server:dev": "npx tsx src/index.ts",
    "server:inspect": "set DANGEROUSLY_OMIT_AUTH=true && npx @modelcontextprotocol/inspector npm run server:dev",
    "client:dev": "npx tsx src/client.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@inquirer/prompts": "^5.5.0",
    "@modelcontextprotocol/sdk": "^1.18.1",
    "zod": "^3.25.76"
  }
}
